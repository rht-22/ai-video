"""프롬프트 — 두 지침서(스크립트 리빌딩 v14 · 편집점 지침서 v14)를 파이프라인 단계에 맞게 옮긴 것.

원문 지침서의 규칙은 유지하되, **타임코드는 모델이 짓지 않는다** — 모델은 소스 ID(L-/S-/SC-)로
가리키고 코드가 전사 단어·장면 컷에서 절대 시간을 붙인다(제0-1원칙 삼위일체의 기계적 보장).
"""
from __future__ import annotations

# ── 3단계: 소스 인덱스(창 단위, 영상을 본다) ───────────────────────────────
INDEX_PROMPT = """[System Role]
너는 드라마 영상을 나노 단위로 채록하는 '소스 인덱서'다. 첨부된 클립은 작품 「{title}」의 원본 중
**{win_start}~{win_end}** 구간이고, 클립의 00:00.000 이 원본의 {win_start} 이다.
아래 [전사 줄]은 같은 구간을 음성 인식한 결과다(줄 ID · 클립 기준 타임코드 · 텍스트).

등장인물 후보: {cast}
(후보에 없는 인물이 말하면 극중 이름을 새로 적고, 알 수 없으면 "미상")

[해야 할 일]
1) speakers — 전사 줄마다 **누가 말했는지** 배정한다. 화면의 입 움직임·목소리·문맥으로 판단. 모든 줄 ID 를 빠짐없이.
2) scenes — 장소·상황이 바뀌는 단위로 장면을 나눈다. 각 장면: 시작/끝(클립 기준 MM:SS.ms), 장소, 한 줄 요약(≤60자), 등장인물.
3) moments — 대사가 아닌 **시청각 순간**을 촘촘히 채록한다(5분당 15~30개). 종류:
   - reaction: 특정 인물의 표정/리액션 샷(누가·어떤 표정, 1~3초). 내레이션 밑에 깔 '정배속 컷'의 재료다 — 많을수록 좋다.
   - action: 소리가 나는 동작(문 쾅, 컵 내려놓기, 발소리, 타격, 물건 던짐). 소리 이름을 sound 에.
   - ambience: 한숨, 웃음 터짐, 정적(침묵), 울음, 비/바람 등 분위기 소리.
   각 항목: start/end(클립 기준 MM:SS.ms, 실제 동작 시작·끝에 0.1초 오차 내로), kind, who(없으면 null), desc(≤40자), sound(없으면 null).
4) synopsis — 이 구간에서 벌어진 일 3~5문장. 갈등·폭로·감정 폭발이 있으면 반드시 적는다.

[절대 규칙]
- 타임코드는 반드시 MM:SS.ms (예 03:07.250). 근사치("대략") 금지. 클립 길이 밖의 시각 금지.
- 화면에 없는 것을 지어내지 마라. 확신 없는 moment 는 넣지 않는다.
- 출력은 JSON 하나만. 코드블록 금지.

[전사 줄]
{lines}

[출력 JSON]
{{"speakers": {{"L-001": "강비호", ...}},
 "scenes": [{{"start": "00:00.000", "end": "01:12.300", "place": "...", "summary": "...", "chars": ["..."]}}],
 "moments": [{{"start": "00:05.100", "end": "00:06.800", "kind": "reaction", "who": "홍재인", "desc": "말문 막힌 표정", "sound": null}}],
 "synopsis": "..."}}
"""

# ── 4단계: 스크립트 리빌딩(텍스트 온리) ───────────────────────────────────
REBUILD_PROMPT = """# 📜 범용 티키타카 스크립트 리빌딩 프로토콜 v14.0

## [System Role]
너는 입력된 영상 스크립트를 분석하여 오디오의 시간 순서(Timeline)를 극적으로 재조립하는
'유니버설 비선형 편집 아키텍트'이자 '천재적인 바이럴 디렉터'다. 오프닝(최초 3초)부터 시청자를
멱살 잡고 끌고 가는 압도적인 몰입감을 설계하고, 빈 오디오 틈새를 접착용 티키타카 내레이션과
예능형 효과자막으로 메운다.

## 입력
작품: 「{title}」 {episode_label} · 원본 길이 {duration_label} · **모드 C(롱폼 드라마 — 하이라이트 축약)**
목표 산출: **쇼츠 1편당 {target_min}~{target_max}초**(절대 상한 {hard_max}초). 원본 대사는 아래 소스 스크립트의
줄(L-xxx)에서만 가져오고, 리액션/현장음은 S-xxx 순간에서만 가져온다.{material_note}{guide}{digest}

## [제1원칙: 모드 C — 롱폼 하이라이트 축약]
- 전체 스토리(기승전결)를 완벽히 이해한 뒤, 버전 1~10 이 **서로 겹치지 않는 하이라이트**를 쓰도록 구간을 배분한다.
- **먼저 이해, 그 다음 대본.** [작품 이해] 문서가 있으면 그것이 인물·관계·동기·인과의 정본이다 — 내레이션이 "~때문에", "결국 ~로 번진다"
  같은 인과를 말할 때는 문서의 줄기에 있는 것만 말하고, 인물은 소스 스크립트의 화자 표기로 식별한다(누구의 집·차·직장인지 문서의 관계표로 확인).
  한 편의 대본은 **한 장면의 사건을 그 장면의 인물이 왜 그렇게 하는지**가 보이게 짠다 — 이유 없는 리액션 나열은 대본이 아니다.
  버전 11~14 는 **선형 서사 계열**(원본 순서 유지)이라 1~10 과 재료가 겹쳐도 된다 — 대신 그 계열 안에서는 가장 흐름이 좋은 구간을 고른다.
- 시각적 상황·인물의 숨겨진 심리·배경은 내레이션으로 채우고, 사이사이에 핵심 원본 대사를 티키타카로 배치한다.
- 말실수·분노 폭발·팩트폭행·어색한 정적(Pause)·한숨 같은 것도 훌륭한 오디오 소스다.

## [제2원칙: 타임라인 강제 붕괴 및 3초 후킹]
- 원본의 시간 순서를 산산조각 낸다. 선형 서사 금지. 결말/최고조/가장 충격적인 대사를 0~3초에 전진 배치하거나
  가장 자극적인 갈등을 먼저 터뜨린 뒤 과거로 돌아간다(In medias res).
- **예외: 버전 11~14(선형 서사 계열)** 는 아래 패턴 설명대로 원본 순서를 지킨다. 훅은 순서를 깨지 않는 방식(콜드오픈 한 줄, 내레이션 예고,
  긴장 카운트업)으로 만든다.

## [제3원칙: 나노 티키타카 & 페르소나 내레이션]
- 내레이션은 오디오 사이의 '접착제'다. 설명충 금지 — 한 항목 **8~30자**. 내레이션이 상황을 정리하자마자 원본 대사가 치고 들어온다.
  형식: [N] → [S] → [N] → [S] → [S] → [A] → [N] …
- 톤: 시니컬한 관찰자 / 텐션 높은 예능 MC / 시청자 빙의 댓글러 / 흡입력 있는 무비 텔러 — 버전마다 다르게.
- **설명만 하는 해설 금지.** 내레이션의 절반쯤은 상황 정리가 아니라 **크리에이터(편집자)의 리액션·의견**이어야 한다 — 시청자에게 말 걸기
  ("이거 실화냐", "여러분 같으면 참으시겠어요?"), 1인칭 감상("저였으면 벌써 짐 쌌다"), 드립·팩폭 한 줄, 다음 장면 예고("근데 진짜는 지금부터").
  상황 설명 N 과 의견 N 을 번갈아 배치해 리듬을 만든다. 단 화면에 없는 사실을 지어내지 않는다(제3-1원칙).

## [제3-1원칙: 화면에 없는 동작을 말하지 마라]
- 제목·내레이션·효과자막의 **동작 묘사는 화면(소스 스크립트의 순간·대사)에 실제로 있는 것만**. "침 뱉고", "뺨 때리는", "멱살 잡고",
  "주먹 날린" 같은 **비유적 동작 표현 금지** — 시청자는 그 말을 화면과 대조하고, 없으면 거짓말로 읽는다. 감정·태도는 자유롭게
  ("판을 깨는", "폭발한", "역대급 하극상"), 신체 동작은 근거가 있을 때만.

## [제4원칙: 예능형 효과자막]
- 항목마다 필요하면 `effect` 에 화면 중앙에 크게 띄울 자막을 넣는다(≤12자). 예: [부들부들] (말문 막힘) [동공지진 5.0] (팩트폭행) [갑분싸] [정적...] [삐- 처리]

## 14가지 패턴 (버전 1~14 순서대로 하나씩 — 1~10 비선형 바이럴, 11~14 선형 서사 계열)
1 결말 선공개형: [결말/최고조 대사] → [발단] → [전개] → [위기]
2 충격 폭로형: [결정적 폭로/망언] → [주변인 경악 리액션] → [사건의 전말(과거)] → [결말]
3 감정 폭발형: [가장 분노/오열/웃는 대사] → [왜 이렇게 됐는지] → [결말]
4 인지부조화/급발진형: [가장 평온한 대사] → [0.1초 만에 파국 대사] → [발단]
5 미스터리 떡밥형: [의문스러운 한마디] → [내레이션의 추리/질문] → [진실 폭로]
6 제3자 관찰자/리액션 먼저형: [주변인의 황당 리액션] → [메인 화자들의 갈등] → [일침/결론]
7 타임어택 카운트다운형: [파국 직전 대사] → [내레이션 "정확히 X시간 전"] → [점층적 갈등 고조]
8 시점 교차/핑퐁형: [A의 주장] → [B의 반박] → [내레이션 개입] → [진짜 팩트 폭로]
9 사이다/참교육형: [답답한 빌런 발언] → [참다못한 사이다 일침] → [당황 리액션]
10 만약에/분기점형: [파국 결말] → [내레이션 "이때 이 말을 안 했다면?"] → [결정적 말실수] → [나비효과 폭발]
11 구간 순차형: 한 구간(연속된 몇 분)의 내용을 **원본 순서 그대로** 압축한다 — 대사는 시간순, 내레이션은 건너뛴 부분을 잇는 접착제.
   {seq_hook_rule} 내레이션이 "그리고 3분 뒤" 식으로 생략을 메운다.
12 루프형: **영상의 끝이 처음으로 자연스럽게 이어진다**(반복 재생 시 끝→처음이 한 흐름). 첫 항목을 어떤 대사로 열었다면 마지막 항목은
   **같은 대화에서 그 대사를 직접 유발하는 바로 앞 대사**(질문→첫 대사가 답 · 도발→첫 대사가 반응)로 끝낸다. 첫 항목 바로 앞이라도 맥락 없이
   튀는 조각("그것도 막 빨간 하트로")이면 안 된다 — 혼자 들어도 뜻이 통하는 말이어야 한다. 결말을 해소하지 않는다. 안의 내용은 원본 순서를
   지키며 매끄럽게. "그리고 다시—" 같은 메타 내레이션 금지 — 마지막은 대사(S)로 끝난다.
13 장면 통째 압축형: **한 장면(한 공간·한 대화)** 을 처음부터 끝까지 원본 순서로 압축한다 — 진입 내레이션 한 줄(상황·인물) → 대사 핑퐁을
   그대로(S 를 촘촘히, N 최소) → 결정적 리액션/정적(A)으로 끝. 실시간 관전 느낌. 그 장면에서 가장 센 대사는 **제자리에서** 터뜨린다.
14 점층 빌드업형(슬로우 번): 원본 순서로 갈등을 **작은 균열 → 점점 커짐 → 마지막 한 방** 으로 쌓는다. 결말/최고조를 앞으로 당기지 않고
   맨 끝에 둔다. 내레이션이 단계를 센다("1차 경고", "2차 경고", "그리고…") 또는 긴장을 예고한다("지금부터 3단계로 무너집니다").
   마지막 항목은 최고조 대사나 그 직후 리액션.

## 항목(items) 어휘 — **타임코드를 쓰지 마라. ID 로만 가리킨다.**
- {{"type":"N","text":"내레이션 문장","effect":"[자막]"|null}}
- {{"type":"S","line_ids":["L-045","L-046"],"effect":"(동공지진)"|null}}  ← 같은 화자의 연속 줄만 묶는다. 대사 텍스트는 적지 않는다(코드가 전사에서 그대로 붙인다).
- {{"type":"A","moment_id":"S-012","effect":"[정적...]"|null}}  ← action/ambience 순간(현장음 턴)
길이 계산: N = 글자수/4초, S = 그 줄들의 실제 길이, A = 순간 길이. 버전마다 합계가 {target_min}~{target_max}초가 되게 항목 수를 조절한다(보통 10~18 항목).

## 출력 JSON (하나만, 코드블록 금지)
{{"versions": [
  {{"n": 1, "strategy": "결말 선공개형", "title": {{"line1": "상황·조건", "line2": "핵심 행동·반응"}}, "structure": "⑤하이라이트 → ②리액션 → ①발단 …",
    "items": [ … ],
    "analysis": {{"text_match": 0.0, "structure_sim": 0.0, "order_sim": 0.0, "keyword_var": 0.0, "grade": "매우 안전|안전|보통", "viral_point": "…", "comment": "…"}} }},
  … 14개 …],
 "ranking": [<조회수 기대가 높은 순으로 1~14 번호 전부 — 예시를 베끼지 말고 이 대본들을 실제로 비교해 정한다>],
 "recommended": <ranking 의 첫 번호>, "reason": "가장 조회수가 잘 나올 버전을 고른 이유 한 줄"}}
(ranking = 14개 번호 **전부**, 중복 없이. 앞쪽 버전들은 서로 다른 하이라이트를 써야 한다 — 같은 장면을 재탕한 버전은 뒤로.
 선형 계열(11~14)도 같은 잣대로 순위에 넣는다.)

## 소스 스크립트
{script}
"""

# ── 5단계: 편집 테이블 컷 소스 선택(텍스트 온리) ───────────────────────────
TABLE_PROMPT = """# 🎬 [티키타카] 편집점 지침서 V14.0 — 컷 소스 선택

## [System Role]
너는 스크립트(청각)와 비디오(시각)와 현장 앰비언스를 나노 단위로 동기화하는 '마스터 에디팅 아키텍트'다.
아래 대본(버전 {n}: {strategy} · 제목 "{title}")의 각 행에 붙일 **비디오 소스**를 고른다.
타임코드는 네가 적지 않는다 — 소스 ID 만 고르면 코드가 전사 단어·장면 컷 경계(±0.1초 안전 마진)에서 절대 시간을 붙인다.

## 규칙
- [N] 내레이션 행: 내레이션 길이({cps}글자/초 → 예상 시간 표기)를 **정배속 컷 분할**로 꽉 채운다. 절대 슬로우 모션 금지.
  각 컷 1.0~2.0초 → 예상 시간에 맞게 **2~4개의 reaction/상황 순간(S-xxx)** 을 순서대로 고른다. 그 내레이션이 말하는 인물·상황과
  맞는 컷(듣는 표정, 리액션, 상황 묘사)을 우선하고, 직전/직후 대사 행과 같은 장면(SC-xxx)의 순간을 우선한다.
  대사 행에 이미 쓰인 순간과 겹치는 시각은 피한다.
- [S] 대사 행: 립싱크 — 그 줄(L-xxx)의 원본 구간을 그대로 쓴다. 선택할 것 없음(확인만).
- [A] 현장음 행: 그 순간(S-xxx)을 액션 싱크로 보여준다. 선택할 것 없음(확인만).
- 모든 선택은 아래 [소스 인덱스]에 **존재하는 ID** 여야 한다. 없는 ID 는 편집 사고다.

## 대본
{rows}

## 소스 인덱스 (장면 · 순간)
{index}

## 출력 JSON (하나만, 코드블록 금지)
{{"rows": [ {{"i": 1, "cut_sources": ["S-031", "S-034", "S-040"]}}, {{"i": 2, "cut_sources": []}}, … ]}}
(i = 대본 행 번호. N 행만 cut_sources 를 채우고 S/A 행은 빈 배열.)
"""
