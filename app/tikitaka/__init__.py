"""티키타카 쇼츠 파이프라인 — 두 지침서(스크립트 리빌딩 v14 · 편집점 지침서 v14)의 이식.

기존 14단계(v1)·v3 와 **독립된 새 파이프라인**이다. 공유하는 것은 로직 조각뿐
(ffmpeg 탐색·TTS 백엔드·장면 전환 검출 방식) — 단계 구성·프롬프트·산출 형식은 여기서 정한다.

단계
  0 probe      소스 프로브 · 스캔 프록시(360p/1fps) · 16k 모노 wav
  1 transcribe faster-whisper 단어 타임스탬프 → 대사 줄(L-xxx) 인덱스
  2 scenecut   ffmpeg scene 검출 → 컷 경계 목록(±0.1s 안전 마진의 기준)
  3 index      Gemini 가 영상을 보고 화자 배정 · 장면 단위 · 리액션/액션/앰비언스 순간 채록 → 소스 인덱스(S-xxx)
  4 rebuild    리빌딩 프로토콜 — 10가지 바이럴 패턴 대본 (텍스트 온리)
  5 table      편집점 지침서 — 선택 버전의 마스터 편집 테이블(N/S/A · MM:SS.ms), 코드가 타임코드를 격자에 묶는다
  6 render     TTS(내레이션) + ffmpeg 타임라인 렌더(1080×1920) + 자막(대사·내레이션·효과자막)

시간 정본 원칙: LLM 은 소스 ID 로 **제안**만 하고, 확정 타임코드는 코드가 전사 단어·장면 컷에서
끌어온다(삼위일체 = [소스 ID]+[타임코드]+[내용] 이 하나의 레코드에 묶여 있어야 편집 테이블에 오른다).
"""
