"""pipeline v4 — 후보 깔때기 파이프라인.

기획 정본 `docs/v4/v4-plan.md`(정리판) · 인터페이스 계약 `docs/v4/M1-interfaces.md` ·
실측 `docs/v4/v4-pipeline-plan.md` §2 와 `docs/v4/probes/`.

원본 회차 하나를 통째로 한 번 이해해 쇼츠 후보를 여럿 뽑고, 화면을 안 봐도 아는 결함은
코드로 거르고, 남은 후보만 화면을 다시 보고 **결함 게이트를 통과한 편을 전부** 완성한다.

    1 init → 2 research → 3 자막 전사 → 4 probe(눈금·fps) → 5 업로드(720p/30fps)
    → 6 후보 편성 → 6b 경계 정밀 → 6c 구간 검증 → 7 깔때기 → 8 시각 플래그
    → 9 승인 → 10 살붙이기 → 11 재료·초벌·스타일·최종·검증        (11 은 승인 편마다)

원칙 다섯(기획서 §1):
  ① 모델은 관찰하고 제안한다. 판단은 코드가 한다(순위·승인·탈락은 순수 함수).
  ② 시간은 전사로 검증한다(6c — 모델이 낸 구간의 대사가 전사에 실제로 있는가).
  ③ 절대 임계는 결정적 사실에만(소스 밖·예고편·발화 없음). 점수 임계는 없다.
  ④ 사람이 이긴다 — 편집실 값은 어떤 자동 판정도 덮지 않는다.
  ⑤ 파일 모양은 바꾸지 않는다 — 현지화·편집실·집계가 읽는 이름·모양은 그대로.

M1 범위는 **바닥**이다: 단계 표(`steps`) · 표본 fps 계단(`fps`) · 구간 검증(`verify`) ·
깔때기(`funnel`) · 승인 게이트(`approve`). 영상을 부르는 단계(6·6b·8·10·11)와 배선은
Wave 2 의 일이다.
"""
