# ⚠️ Python 3.10 이상 3.14 미만에서 설치할 것 (pyproject.toml 의 requires-python 참고).
#    아래(3.9): 핀 중 fastapi/uvicorn/alembic/pydantic-settings/python-dotenv/python-multipart 는
#               3.9 에서 해석되지 않아 조용히 낮은 버전으로 깔린다.
#    위(3.14): deepface 가 끌어오는 tensorflow 가 cp313 까지만 휠을 내서 설치가 아예 실패한다.
#    실측 통과 조합: Python 3.12 (2026-07-31, 전체 의존성 해석 성공)
#
# ⚠️ ffmpeg 는 pip 패키지가 아니라 시스템 바이너리다. **7.x 를 쓸 것 — 8.x 는 미지원.**
#    8.x 는 자막 필터(`ass=…:fontsdir=…`)와 `-filter_complex_script` 문법을 거부해 렌더가 실패한다.
#    macOS: `brew install ffmpeg@7` (앱 시작 시 app/modules/ffmpeg_utils.py 가 버전을 검사한다)

google-genai>=0.8.0
edge-tts>=6.1.9
opencv-python>=4.9.0.80,<5   # 5.x 는 번들 haarcascade 를 제거해 얼굴검출(reframe) 단계가 죽는다
faster-whisper>=1.0.0

# Web Framework & Server
fastapi==0.135.1
uvicorn[standard]==0.41.0

# Database & Migration
SQLAlchemy==2.0.48
PyMySQL==1.1.2
alembic==1.18.4

# Environment Settings
pydantic==2.12.5
pydantic-settings==2.13.1
python-dotenv==1.2.2

# Security & Authentication
python-jose[cryptography]==3.5.0
passlib==1.7.4
python-multipart==0.0.22

# Utilities
typing-extensions==4.15.0
watchfiles==1.1.1
requests>=2.31.0
# 로고 자산 처리(여백 트림·잉크 bbox 측정)와 폰트 패밀리명 검증(tests/test_font_family.py)에
# 쓴다. deepface 가 전이로 끌어와서 지금까지 우연히 동작했지만, 그쪽 의존성이 바뀌면 조용히
# 사라진다 — 직접 쓰므로 명시한다.
Pillow>=10.0.0

# Face Recognition (Phase 12)
deepface>=0.0.93

# Media / Source download
# app/modules/youtube_downloader.py 가 `import yt_dlp` 한다. 이게 없으면 --youtube-url 경로가 즉사한다.
# 2025.10.14 이하는 YouTube 다운로드가 깨진다("The page needs to be reloaded") — py3.9 에서는
# 그 버전이 상한이라 설치 자체가 실패해야 정상이다(조용히 깨진 버전을 쓰는 것보다 낫다).
yt-dlp>=2026.1
gdown>=5.0

# Dev
pytest>=8.0
