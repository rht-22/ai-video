google-genai>=0.8.0
edge-tts>=6.1.9
opencv-python>=4.9.0.80
faster-whisper>=1.0.0

# Web Framework & Server
fastapi==0.135.1
uvicorn[standard]==0.41.0

# Database & Migration
SQLAlchemy==2.0.48
PyMySQL==1.1.2
alembic==1.18.4

# Environment Settings
pydantic==2.12.5
pydantic-settings==2.13.1
python-dotenv==1.2.2

# Security & Authentication
python-jose[cryptography]==3.5.0
passlib==1.7.4
python-multipart==0.0.22

# Utilities
typing-extensions==4.15.0
watchfiles==1.1.1
requests>=2.31.0
# 로고 자산 처리(여백 트림·잉크 bbox 측정). deepface 가 전이로 끌어와서 지금까지 우연히 동작했지만,
# 그쪽 의존성이 바뀌면 조용히 사라진다 — 직접 쓰므로 명시한다.
Pillow>=10.0.0

# Face Recognition (Phase 12)
deepface>=0.0.93